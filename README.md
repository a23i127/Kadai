# 📂 GitHub APIを使ったファイルエクスプローラー

このプロジェクトは **個人開発** ですが、  
インターンでのチーム開発で学んだことをアウトプットしながら勉強する方針で進めています。

---

## 🌿 ブランチ運用

10月16日に `main` に直接マージしてしまいました（申し訳ないです）。  
以降は、**developmentブランチからPull Requestを出して差分説明を行う方針**にしています。

---

## ⭐️ システムフロー全体図

![システムフロー](https://github.com/user-attachments/assets/ed1d2409-29b7-49e5-8093-9b54f876d23e)

---

## 🧩 主な構成

### 🖥 Frontend
- `DisplayArea.tsx` がエントリポイントで、`localhost`アクセス時に表示される。  
- バックエンドのAPIを叩いて、`Organization` が `google` に設定されているリポジトリを管理。  
- GitHubサイトを開かなくても、リポジトリを閲覧できるようにしている。  

### ⚙️ Backend
- GitHubが提供しているAPIを叩く処理を実装。  
- リポジトリアクセス時にDBへ保存し、次回はDBから読み込むキャッシュ構造にしている。  

---
🎯 要件定義（Requirements）

本プロダクトでは、「GitHub リポジトリ閲覧体験の効率化」を目的として、
以下の機能要件を満たす設計・実装を行っています。

⸻

1. 📡 GitHub API からリポジトリ情報を取得できること
	•	GitHub REST API を利用し、指定組織（例：google）のリポジトリ情報を取得する。
	•	初回アクセス時は必ず API を叩き、最新のリポジトリデータを取得する。
	•	API 通信では、ページネーション対応・エラーハンドリングを適切に行う。

⸻

2. 💾 一度取得したリポジトリは DB にキャッシュし、再取得時は DB を優先する
	•	取得したリポジトリは DB に保存（キャッシュ）する。
	•	次回以降は DB を優先して読み込み, API コールの回数と通信コストを削減する。
	•	「初回のみ API、2回目以降はキャッシュ利用」構造でパフォーマンス最適化。
	•	ただし ファイル内容(content) は API からでないと取得できないため例外的に API 優先。

⸻

3. 🔍 リポジトリ検索機能を実装する
	•	キャッシュ済みデータ（DB）を対象に検索可能にすることで、高速検索を実現。
	•	ユーザー体験向上のため、検索候補サジェストも実装。
	•	大量のデータでもストレスなく検索可能な UI/UX を意識。

⸻

4. ⭐ お気に入り（Favorite）機能をつける
	•	各リポジトリに対して「お気に入り」を付与できる。
	•	お気に入り状態は UserRepository テーブルで管理。
	•	ブラウザ閉じた時に自動保存されるようにし、保存操作を意識させない設計。
	•	保存はバルクインサートで一括処理して DB 負荷を軽減。

⸻

5. 🏷️ タグ機能を実装する
	•	リポジトリにユーザー固有のタグを紐づけできる。

⸻

   
## 💡 工夫した点

- 一度閲覧したリポジトリの情報は、**バルクインサート**のようにまとめてDBに保存し、  
  クエリ数を最小化してパフォーマンスを向上させた。  
- **miro**を活用して要件整理を行い、効率的な開発を実現。  
- 個人開発でもチーム開発を意識してディレクトリを整理し、  
  機能ごとにファイルを分割して保守性を向上。  
- ファイル分割によりデバッグ効率が格段に向上。
- 保存する機能は、ブラウザを離れる時に自動でキャッシュに保存するようにして、ユーザーから保存の意識がなくてもアプリケーション内でキャッシュを活用することが可能になった。
- キャッシュに保存するのはブラウザを閉じた後のみなので、お気に入りのトグル処理や、タグ付けなど取得したリポジトリに対しての操作をしても、ユーザーの最終的な意図した操作でキャッシュに保存することができるようになった。またキャッシュに保存するのはバルクインサートで一気に保存するようにしているため、db負担も少なく、ユーザーのリポジトリ保存処理にかかるクエリの数を減らし、パフォーマンス向上を考えて開発できたこと。
---

## 😣 妥協点・悔しい実装

- ファイルの中身（ソースコード部分）は、GitHub APIの都合上、  
  「ファイルを開かないと取得できない」ため、  
  未開封ファイルのキャッシュデータは `null` になってしまう。  
- そのため、キャッシュ優先で取得していたロジックでは、  
  初回時に中身が表示できない問題が発生。  
  → **ファイル内容はAPI優先で取得**するよう方針を変更。  

### 🧠 本当はやりたかった実装
- ファイルディレクトリ情報はキャッシュで取得し、  
  ファイル内容表示時に `content` が `null` ならAPI叩いてDB更新、という流れを作りたかった。  

---

## 🔧 改善点（反省）

### 1. 基盤作りに時間をかけすぎてしまった
デバッグを AI 任せにしてしまい、  
結果として問題箇所の把握や修正に余分な時間を使ってしまった。  
今後は **テストによる安全性確保** と **手動デバッグの徹底** を行い、  
AI に依存しすぎない開発体制を整える。

---

### 2. テストを手動で行っていたことで非効率になっていた
すべて手動でテストしていたため、  
開発コストが増え、生産性を圧迫してしまった。  
この経験から **自動テスト導入の重要性** を強く実感した。  
自動化できる部分は自動化し、本質的な課題に集中できる環境を整えたい。

---

### 3. LLM を使ったコーディングの進め方に課題があった
技術選定後にコーディングへ進む際、  
デバッグしやすさを考えずに実装を進めてしまい、  
デバッグ段階で **自分の実装意図を再理解するところからやり直す** という非効率が生じた。

この経験から、実装前に以下を必ず確認する重要性を認識した：

- 関数やメソッドのシグネイチャ  
- データの流れ  
- 例外ケースや境界値

LLM に任せるのではなく、  
**「自分が後で修正できるコードか」** を基準に使いこなす姿勢を徹底していく。

4. テーブル設計で RDB の強みを活かせていなかった反省

今回の開発を通して、リレーショナルデータベース（RDB）の本来の強みを十分に活かせていなかった ことに気づきました。
	•	既存のテーブル設計では、正規化が十分に行われていなかった
	•	1つのエンティティ（例：Repository, Tag, User）に対して、
複数の関連データを持たせる設計ができておらず,
RDBを使用する意味が曖昧な状態で開発が進んでいた
	•	その結果、データの拡張性・柔軟性が低く、
機能追加のたびにテーブル見直しが必要になる構造だった

この問題を改善するために、
中間テーブルを活用し、多対多の関係を正しく表現する設計へ全面的に作り直しました。

その結果、
	•	1つのデータ（User, Repository）に対して複数のタグを持たせたり
	•	同じリポジトリに複数ユーザーがタグ付けしたり
	•	お気に入り状態やタグ付け状態を柔軟に管理できる

といった、RDB の強みを最大限活かした拡張性の高いアーキテクチャを実現できました。

今回の作り直しを通じて、

「RDB は中間テーブルを使って、関係を柔軟に表現できるからこそ強い」

ということを深く理解できました。
これまであまり意識できていなかったポイントで、非常に大きな学びとなりました。

##newModel.goより(model.goを改善してみました。)

type User struct {
	ID        uint      `gorm:"primaryKey" json:"id"`
	Username  string    `gorm:"uniqueIndex;not null" json:"username"`
	Email     string    `gorm:"uniqueIndex;not null" json:"email"`
	Password  string    `gorm:"not null" json:"-"` // JSONには含めない
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}

// Tag テーブル - ユーザー固有のタグ
type Tag struct {
	ID     uint   `gorm:"primaryKey" json:"id"`
	UserID uint   `gorm:"index;not null" json:"user_id"`
	Name   string `gorm:"not null" json:"name"`
	Color  string `gorm:"default:#3498db" json:"color"` // タグの色

	// 外部キー制約
	User User `gorm:"foreignKey:UserID" json:"user,omitempty"`

	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`

	// 複合ユニークインデックス（同じユーザーが同じ名前のタグを複数作成できない）
	_ struct{} `gorm:"uniqueIndex:user_tag_name,user_id,name"`
}

// UserRepository テーブル - ユーザーとリポジトリの関連（お気に入り等）
type UserRepository struct {
	ID           uint `gorm:"primaryKey" json:"id"`
	UserID       uint `gorm:"index;not null" json:"user_id"`
	RepositoryID uint `gorm:"index;not null" json:"repository_id"`
	IsFavorite   bool `gorm:"default:false" json:"is_favorite"`

	// 外部キー制約
	User       User       `gorm:"foreignKey:UserID" json:"user,omitempty"`
	Repository Repository `gorm:"foreignKey:RepositoryID" json:"repository,omitempty"`

	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`

	// 複合ユニークインデックス（同じユーザーが同じリポジトリを複数回関連付けできない）
	_ struct{} `gorm:"uniqueIndex:user_repository,user_id,repository_id"`
}

// RepositoryTag テーブル - リポジトリとタグの多対多関係
type RepositoryTag struct {
	ID           uint `gorm:"primaryKey" json:"id"`
	UserID       uint `gorm:"index;not null" json:"user_id"`
	RepositoryID uint `gorm:"index;not null" json:"repository_id"`
	TagID        uint `gorm:"index;not null" json:"tag_id"`

	// 外部キー制約
	User       User       `gorm:"foreignKey:UserID" json:"user,omitempty"`
	Repository Repository `gorm:"foreignKey:RepositoryID" json:"repository,omitempty"`
	Tag        Tag        `gorm:"foreignKey:TagID" json:"tag,omitempty"`

	CreatedAt time.Time `json:"created_at"`

	// 複合ユニークインデックス
	_ struct{} `gorm:"uniqueIndex:user_repo_tag,user_id,repository_id,tag_id"`
}

// 既存のRepository構造体を修正
type newRepository struct {
	ID            uint   `gorm:"primaryKey" json:"id"`
	Name          string `gorm:"index;not null" json:"name"`
	FullName      string `gorm:"uniqueIndex;not null" json:"full_name"`
	DefaultBranch string `json:"default_branch"`
	// Tag フィールドを削除（多対多関係で管理）
	Owner     Owner     `gorm:"embedded;embeddedPrefix:owner_" json:"owner"`
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
}

---
## 📝 要件定義とテーブル設計に関する振り返り

###  要件定義時に DB の使用方法を明確にしないままテーブル設計してしまった

今回の開発では、**DB をどのように使うか（扱うデータの種類・役割）を具体的に定義しないまま、感覚的にテーブルを分割してしまった**という課題がありました。

実装後に振り返ってみると、テーブル設計の段階でロジックが曖昧だったため、以下のような問題につながっていたことに気づきました。

- 「なぜこのテーブルを分けているのか」が不明瞭  
- 機能との整合性が弱く、実装時に迷いが発生  
- 拡張時にテーブルそのものを見直す必要が出てしまう  
- プロダクト全体での DB の役割を十分に意識できていなかった  

---

### ✅ 改善のために行ったこと

要件を一度立ち返って整理し、

- **DB がプロダクト内で果たす役割は何か**
- **どの種類のデータに対して機能を実装する必要があるか**
- **永続化・検索・更新・拡張の観点で何が必要か**

を洗い出しました。

その結果、  
テーブルを分ける際の判断基準として

> 「拡張性を意識しながら、どの単位でデータを分離すると管理しやすいか」

といった **具体的なロジック**が明確化され、  
テーブル設計にも一貫性が生まれました。

---

### ✅ 得られた学び

今回の振り返りで、以下の点を学びました。

- テーブル設計は **要件が明確になってから行うべき**
- 完璧を求めて、プロトタイプは作る必要はないが,db運用の方向性をしっかり定めるためにも、要件から洗い出す時間はとても大事！！  
- プロトタイプ段階でも、DBの役割と扱うデータ種別を意識すると開発がスムーズになる  
- “なんとなく” の分割は後の修正コストにつながる  
- 要件をクリアにすると、拡張性を持ったデータ設計ができる  

---

### まとめ

今回の経験を通じて、

> **「テーブル設計は感覚ではなく、プロダクト内のデータの役割とロジックに基づいて行うべき」**

という基本を改めて実感しました。

今後は、要件定義の段階で  
DB がどのように使われるのかを丁寧に洗い出し、  
プロダクト全体の運用方針を意識した設計を行っていきます。

## ✅ 全体的な振り返り

LLM はプロダクトを効率的に進める非常に強力なツールだが、  
使いこなし方によって **開発体験や生産性が大きく変わる** ことを実感した。

任せきりにすると一見動くコードは出るが、  
理解を伴わないため改修・デバッグコストが増え、  
本来注力すべき部分に時間を割けなくなる問題があった。

今後は、LLM を  
**「便利な自動生成機」ではなく、自分の技術力を拡張するツール」**  
として扱えるように、基礎的な設計力・デバッグ力を磨きつつ、  
LLM を使いこなすスキルも並行して高めていく。
---

## 💎 良かったところ

- 基盤完成後は、ファイル分割を行っていたおかげで、  
  検索機能・お気に入り機能などをスムーズに追加できた。  
  → **依存度の低減・分割設計の効果を体感。**  
- AI活用時の技術選定意識が変化。  
  → 「とりあえず動けばいい」ではなく、  
    **パフォーマンス・選定理由を説明できる設計**を意識するようになった。  
- **デバッグが好きになった！**  
- **AIとデバッグの併用**で、効率的に開発できることを実感。  

---

## 🧪 追加実装：検索しやすいように検索候補機能を作った

- 検索する際に、キャッシュを活用した検索候補機能を実装し、一回見たリポジトリの再検索をしやすくした
---
## 時間外で実装したこと
  - 追加読み込み機能の実装
  - キャッシュデータが空の時だけでなく、取得したfileのcontentが空の場合も、apiを叩いて表示できるように機能拡張をした。(上の本当はやりたかった実装のこと)
  - バックエンドapiテストの自動化 ./run_all_tests.sh




## 🧭 学び

- API実装時、**エラー原因がフロントかバックかを行き来して確認する**作業に時間がかかっていた。  
- バックエンドテストを自動化することで、**時間効率の大幅改善**を実感。  
- 運用はまだしていないが、将来的に**自動テストが安全性や信頼性の基盤になる**と感じた。  
- 今後は **テスト自動化を前提にした開発** を意識して取り組んでいきたい。
- 技術選定を行うだけではなく、シグネイチャやデータの流れも理解しながら開発を進めることで、デバックにかかる時間を大幅に削減することができるので、必ず意識しながらLLMを扱うこと。
- 制限時間を意識して作業する中で、限られた時間を最大限活用するには 開発プロセスの見直しが不可欠 だと気づいた。 
- envなどの扱い方や、チーム開発におけるenvの共有の仕方などを知ることができた。



